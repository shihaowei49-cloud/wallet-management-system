# 🚀 Zeabur 部署指南

将 Wallet Management System 部署到 Zeabur 的完整教程。

## 📋 部署前准备

### 1. 注册 Zeabur 账号
访问 https://zeabur.com 并注册账号（支持 GitHub 登录）

### 2. 准备代码
确保你的代码已推送到 GitHub 仓库

```bash
cd wallet-management-system

# 初始化 git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 关联远程仓库
git remote add origin https://github.com/你的用户名/wallet-management-system.git

# 推送
git push -u origin main
```

## 🔧 部署步骤

### 第一步：部署后端

1. **创建新项目**
   - 登录 Zeabur Dashboard
   - 点击 "Create Project"
   - 输入项目名: `wallet-management`

2. **部署后端服务**
   - 点击 "Add Service"
   - 选择 "Git"
   - 选择你的 GitHub 仓库
   - 选择 `backend` 目录
   - 服务名称: `backend`

3. **配置环境变量**
   点击后端服务 → Settings → Environment Variables，添加：

   ```env
   PORT=5001
   JWT_SECRET=你的超级安全密钥-必须修改
   NODE_ENV=production
   ```

4. **获取后端域名**
   - 点击 "Networking" → "Generate Domain"
   - 记下域名，例如: `backend-xxx.zeabur.app`

### 第二步：部署前端

1. **添加前端服务**
   - 在同一项目中点击 "Add Service"
   - 选择 "Git"
   - 选择你的 GitHub 仓库
   - 选择 `frontend` 目录
   - 服务名称: `frontend`

2. **配置前端环境变量**
   点击前端服务 → Settings → Environment Variables，添加：

   ```env
   VITE_API_URL=https://你的后端域名/api
   ```

   例如:
   ```env
   VITE_API_URL=https://backend-xxx.zeabur.app/api
   ```

3. **配置构建命令**
   Zeabur 会自动检测 Vite 项目，但你也可以手动配置：
   - Build Command: `npm run build`
   - Output Directory: `dist`

4. **生成前端域名**
   - 点击 "Networking" → "Generate Domain"
   - 可以使用默认域名或绑定自定义域名

### 第三步：配置 CORS

修改 nginx.conf 中的后端地址：

```nginx
location /api {
    proxy_pass https://你的后端域名;
    # ... 其他配置
}
```

## 🔒 安全配置

### 1. 修改 JWT Secret

⚠️ **重要**: 生产环境必须使用强密钥！

```bash
# 生成随机密钥
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

将生成的密钥设置到后端环境变量 `JWT_SECRET`

### 2. 配置 CORS

后端 `src/server.js` 已经配置了 CORS，但建议在生产环境限制域名：

```javascript
app.use(cors({
  origin: 'https://你的前端域名.zeabur.app',
  credentials: true
}));
```

## 📦 使用 Zeabur CLI 部署（可选）

### 安装 CLI

```bash
npm install -g @zeabur/cli
```

### 登录

```bash
zeabur auth login
```

### 部署

```bash
# 部署后端
cd backend
zeabur deploy

# 部署前端
cd ../frontend
zeabur deploy
```

## 🌐 自定义域名

### 绑定域名

1. 在 Zeabur Dashboard 中点击服务
2. 进入 "Networking"
3. 点击 "Add Domain"
4. 输入你的域名 (例如: `wallet.yourdomain.com`)
5. 按照提示添加 DNS 记录

### DNS 配置

在你的域名提供商处添加 CNAME 记录：

```
类型: CNAME
名称: wallet (或 @)
值: xxx.zeabur.app (Zeabur 提供的域名)
```

## 🔍 验证部署

### 1. 检查后端
访问: `https://你的后端域名/health`

应该看到:
```json
{
  "status": "ok",
  "timestamp": "2025-11-22T..."
}
```

### 2. 检查前端
访问: `https://你的前端域名`

应该看到登录页面

### 3. 测试登录
- 用户名: `admin`
- 密码: `admin123`

## 📊 监控和日志

### 查看日志

1. 在 Zeabur Dashboard 中点击服务
2. 点击 "Logs" 标签
3. 实时查看应用日志

### 监控资源

在 "Metrics" 标签中可以看到：
- CPU 使用率
- 内存使用率
- 网络流量

## 🐛 常见问题

### 1. 前端无法连接后端

**检查**:
- 后端域名是否正确配置在 `VITE_API_URL`
- CORS 是否正确配置
- 后端服务是否正常运行

**解决**:
```bash
# 检查环境变量
echo $VITE_API_URL

# 测试后端
curl https://你的后端域名/health
```

### 2. 构建失败

**常见原因**:
- 依赖安装失败
- Node 版本不匹配
- 构建命令错误

**解决**:
在 Zeabur Settings 中检查：
- Node 版本 (推荐 18 或 20)
- 构建日志中的错误信息

### 3. 502 Bad Gateway

**原因**:
- 后端服务未启动
- 端口配置错误

**解决**:
检查后端环境变量中的 `PORT` 是否正确

### 4. 钱包功能无法使用

**原因**:
- ethers.js 等 Web3 库在服务器端运行有限制
- 钱包功能主要在前端执行

**说明**:
- 创建钱包、发送交易等功能在前端执行
- 后端主要提供数据管理接口

## 🔄 更新部署

### 方式一：自动部署

推送代码到 GitHub 后，Zeabur 会自动检测并重新部署：

```bash
git add .
git commit -m "Update features"
git push
```

### 方式二：手动部署

在 Zeabur Dashboard 中：
1. 点击服务
2. 点击 "Redeploy"
3. 选择 "Latest Commit"

## 💰 定价说明

Zeabur 提供：
- **免费套餐**:
  - 每月 5 美元额度
  - 适合测试和小型项目

- **付费套餐**:
  - Developer: $5/月
  - Team: $15/月
  - 按实际使用付费

查看详情: https://zeabur.com/pricing

## 📚 参考资源

- [Zeabur 官方文档](https://zeabur.com/docs)
- [Node.js 部署指南](https://zeabur.com/docs/deploy/nodejs)
- [静态网站部署](https://zeabur.com/docs/deploy/static)
- [环境变量配置](https://zeabur.com/docs/deploy/variables)

## ✅ 部署检查清单

部署前确认：

- [ ] 代码已推送到 GitHub
- [ ] 修改了生产环境的 JWT_SECRET
- [ ] 配置了正确的环境变量
- [ ] 测试了本地构建 (`npm run build`)
- [ ] 检查了 package.json 中的依赖

部署后验证：

- [ ] 后端 /health 接口正常
- [ ] 前端页面可以访问
- [ ] 登录功能正常
- [ ] API 请求成功
- [ ] 钱包功能正常

---

## 🎉 完成！

你的 Wallet Management System 现在已经部署到 Zeabur 了！

**后端地址**: `https://你的后端域名.zeabur.app`
**前端地址**: `https://你的前端域名.zeabur.app`

享受你的去中心化钱包系统吧！🚀

---

**需要帮助？**
- 查看 [Zeabur Discord](https://discord.gg/zeabur)
- 提交 Issue 到项目仓库
